# Copy to  keystore/keystore.properties  and fill in real values.
# The real file is git-ignored.
storeFile     = golden_flux.jks
storePassword = REPLACE_ME
keyAlias      = golden_flux
keyPassword   = REPLACE_ME
